{
  "name": "storage-waiter",
  "version": "0.1.0",
  "private": true,
  "description": "StorageWaiter — your storage waiter: aggregates multiple free cloud accounts into one virtual drive",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @storagewaiter/app",
    "build": "npm run build -w @storagewaiter/core && npm run build -w @storagewaiter/app",
    "test": "npm run test -w @storagewaiter/core",
    "typecheck": "npm run typecheck -w @storagewaiter/core && npm run typecheck -w @storagewaiter/app"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
